# IFT-2015-TP3
Implementation of Kruskal's algorithm.

## TODO
- [ ] Replace disjoint sets with `import java.util.Collections` and `Collections.disjoint(set1, set2)` and merging sets from `java.util.Set`.
